imports:
  - path: gcs_bucket.py
    name: gcs_bucket.py

resources:
  - name: gcs-bucket-deploy
    type: gcs_bucket.py
    properties:
      name: gcs-bucket-deploy
      location: us-east1
      versioning:
        enabled: False

      

